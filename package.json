{
  "name": "dr-krishna-induvasi-hospital-site",
  "version": "1.0.0",
  "description": "Hospital landing page (frontend) with a Node.js appointment booking API (backend), plus email notifications on new bookings.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "nodemailer": "^6.9.14"
  }
}
