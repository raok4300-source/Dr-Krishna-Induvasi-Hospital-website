{
  "name": "dr-krishna-induvasi-hospital-site",
  "version": "1.0.0",
  "description": "Hospital landing page (frontend) with a Node.js appointment booking API (backend). No external dependencies required.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
